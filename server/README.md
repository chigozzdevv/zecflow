# ZecFlow Server

TypeScript/Node backend powering the ZecFlow automation platform. Configure environment variables in `.env` (see `.env.example`) and run the dev server with:

```bash
npm run dev
```
